import { DocumentSignProcess } from './document-sign-process';

describe('DocumentSignProcess', () => {
  it('should create an instance', () => {
    expect(new DocumentSignProcess()).toBeTruthy();
  });
});
